
def distancia_manhattan(a,b):
    return abs(a.x - b.x) + abs(a.y - b.y)

def greedy(mapa, nodo_inicio, nodo_meta):
    # Implementación del algoritmo Greedy
    # Aquí deberías agregar la lógica para encontrar la ruta desde nodo_inicio hasta nodo_meta
    # utilizando el enfoque Greedy. Por ejemplo, podrías usar una función heurística para
    # seleccionar el siguiente nodo a visitar.
    pass  # Reemplaza esto con la implementación real del algoritmo Greedy